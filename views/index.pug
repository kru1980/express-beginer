extends layout
block main
  if username
    h2 Привет #{username}
    a(class='button' href='/suggestions') Посмотреть список предложений 
  else  
    form(method='POST')
      input(type='text', name='username')
      button(type='submit') Войти
